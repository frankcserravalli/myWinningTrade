//names = "<%=raw @group_users.to_s.html_safe %>";

 names = ["Jason"];
// Here I'm setting up the auto-complete function for student searches
$( "#search" ).autocomplete({
  source : names
});

console.log("<%= @group_users.to_json %>");



